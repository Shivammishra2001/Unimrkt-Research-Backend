{
  "collectionName": "components_blocks_stats_bands",
  "info": {
    "displayName": "Stats Band",
    "icon": "chart-bubble"
  },
  "attributes": {
    "heading": {
      "type": "string",
      "maxLength": 120
    },
    "items": {
      "type": "component",
      "component": "blocks.stat-item",
      "repeatable": true,
      "required": true,
      "min": 1,
      "max": 6
    },
    "theme": {
      "type": "enumeration",
      "enum": ["light", "dark"],
      "default": "light"
    },
    "anchorId": {
      "type": "string",
      "regex": "^[a-z0-9-]+$"
    }
  }
}
